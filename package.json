{
  "name": "popihub-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n \"ENTRY,GENAI\" -c \"cyan,magenta\" \"npm run dev --prefix entry\" \"npm run dev --prefix GenAILife\"",
    "dev:entry": "npm run dev --prefix entry",
    "dev:genai": "npm run dev --prefix GenAILife"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
