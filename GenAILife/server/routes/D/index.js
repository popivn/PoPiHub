import express from 'express';

const router = express.Router();

// [DELETE] Nơi định nghĩa các route xóa dữ liệu sau này
// Ví dụ: router.delete('/player/:id', authController.deletePlayer);

export default router;
