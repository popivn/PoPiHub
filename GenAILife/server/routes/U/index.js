import express from 'express';

const router = express.Router();

// [UPDATE] Nơi định nghĩa các route cập nhật dữ liệu sau này
// Ví dụ: router.put('/user/profile', authController.updateProfile);

export default router;
